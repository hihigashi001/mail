import React from "react";
import { Layout } from "src/layouts/Layout";
// components
import { TableMail } from "src/components/separate/TableMail";

const index = () => {
  return (
    <Layout>
      <TableMail />
    </Layout>
  );
};

export default index;
